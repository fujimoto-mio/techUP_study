◻︎index.php
<?php
//クラスファイルを読み込む
require_once 'Patient.php';
?>
<!DOCTYPE html>
<html lang="ja">
	<meta charset="utf-8">
	<head>
		<title>techUP オブジェクト 指向課題</title>
	</head>
	<body>
<?php
//インスタンスを生成する
$patient = new Patient('山田太郎', 30, 'm', 1.65, 63.5);

print($patient -> selfIntroduction());
print('<br>身長：' . $patient -> getHeight() . 'm');
print('<br>体重：' . $patient -> getWeight() . 'kg');
print('<br><br>');
print('標準体重は' . $patient -> calculateStandardWeight() . 'kgです。');
?>
	</body>
</html>
①
ドクタイプ宣言でバージョンの書き込み
文章表記　日本語の出力
メタ要素宣言で文字コード指定
ページタイトル『オブフェクト　指向　課題』を出力
患者オブジェクト山田太郎、３０、m、１．６５、６３．５を定義。
患者の自己紹介を出力
患者の身長出力
患者の体重出力
『標準体重は　　kgです。』出力

◻︎patient.php
<?php
//クラスファイルを読み込む
require_once 'Person.php';

class Patient extends Person
{
	//プロパティ==========
	private $height = 0;		//身長(m)
	private $weight = 0;		//体重(kg)

	//コンストラクタ==========
	public function __construct($name, $age, $gender, $height, $weight){
		//Personクラスのコンストラクタを呼び出す
		parent::__construct($name, $age, $gender);

		//プロパティの初期化
		$this -> height = $height;
		$this -> weight = $weight;
	}

	//メソッド==========
	//標準体重を返すメソッド
	public function calculateStandardWeight(){
		return $this -> height * $this -> height * 22;
	}

	//身長を返すメソッド
	public function getHeight(){
		return $this -> height;
	}

	//体重を返すメソッド
	public function getWeight(){
		return $this -> weight;
	}
}
②
 Pesonクラスをpatientクラスに承継。
身長、体重のプロパディの宣言。
Patientクラスをコンストラクタし名前、年齢、性別、身長、体重宣言。
親クラスのPesonオブジェクトも実行させるためparent::__constructで名前、年齢、性別の宣言
プロパディの初期化を実行。
標準体重と身長、体重のメソッド宣言。


◻︎person.php
<?php
class Person{
	//プロパティ==========
	private $name = '';
	private $age = 0;
	private $gender = '';

	//コンストラクタ==========
	public function __construct($name, $age, $gender){
		$this -> name = $name;
		$this -> age = $age;
		$this -> gender = $gender;
	}

	//メソッド==========
	public function selfIntroduction(){
		if($this -> gender == 'm'){
			$gendata = '男性';
		}else if($this -> gender == 'f'){
			$gendata = '女性';
		}else{
			$gendata = '[性別は不明]';
		}
		return 'わたしは' . $this -> name . '、' . $this -> age . '才、' . $gendatar . 'です。';
	}


	public function addAge(){
		$this -> age++;
	}
}


③
Pesonクラス定義
名前、年齢、性別　プロパティ宣言
コンストラクタの名前、年齢、性別を宣言
メソッドの呼び出し。
性別が男性だった場合は私は名前、年齢、男性です。を出力
性別が女性だった場合は私は名前、年齢、女性です。を出力
性別は不明だった場合は私は名前、年齢、性別不明です。を出力
年齢を追加するメソッドを定義。


